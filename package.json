{
  "name": "ai-business-builder",
  "version": "1.0.0",
  "description": "Autonomous AI model for building business apps without user input",
  "main": "ai-model.js",
  "scripts": {
    "start": "node ai-model.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@vercel/speed-insights": "^2.0.0"
  }
}
npm install next react react-dom
# then commit the updated package.json and package-lock.json
git add package.json package-lock.json
git commit -m "fix: add next.js as dependency"
git push
